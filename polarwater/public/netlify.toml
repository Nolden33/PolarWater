[build]
  base    = "polarwater"       # Adjust based on your project structure
  publish = "build"  # Directory where your built files are outputted
  command = "CI=false npm run build" # Temporarily bypass CI warnings; remove after fixing ESLint issues

[build.environment]
  NODE_VERSION = "16" # Ensure the Node.js version matches your project requirements

[[plugins]]
  package = "@netlify/plugin-nextjs" # Example of using a plugin, adjust based on your project needs
